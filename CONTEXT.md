# DSH 傻瓜整合包

一个以官方 DSH（DeepSeek Harness）为基座的"发行版外壳"项目：在基座之上组合安装器、启动器、精选插件与预设配置，让非技术用户开箱即用。

## Language

**发行版外壳**（整合包）:
以官方 DSH 为基座、在其之上组合"安装器 + 启动器 + 精选插件 + 预设配置"的可分发产物。
_Avoid_: 深度 fork、插件合集（仅罗列插件而不组合）、配方清单（只给脚本不给安装体验）

**发行层**:
不 fork DSH 内核、以"固定版本 DSH runtime + 自研/下游适配插件"打包分发的架构模式（社区先例：oh-dsh）。
_Avoid_: 内核补丁、逐行维护上游 fork

**基座**:
整合包构建于其上的发行层；已选定 oh-dsh（三形态统一、Pinned runtime、分层分发）。
_Avoid_: 从官方 DSH 自建发行层

**许可政策**:
宽松——许可状态不作为选品门槛；如出现侵权投诉，按"侵删"处理。
_Avoid_: 许可卫生审查、以许可为由排除插件

**三界面**:
webui（浏览器）、gui（桌面应用）、tui（终端）——同一核心的三套前端，共享同一份数据与配置，用户启动时自选。
_Avoid_: 三个独立产物

**轻量化**:
三项指标按优先级排序的取舍原则：功能唯一性 > 资源开销 > 包体积。
_Avoid_: 笼统的"越小越好"

**功能唯一性**:
每个功能只保留一个实现的原则；现有实现都不够好时，允许融合多个实现成一个新插件。
_Avoid_: 多实现并存、把开关权全部交给用户

**精选插件**:
通过"每功能一个实现"规则选出、默认启用的插件。
_Avoid_: 未筛选的全部社区插件

**核心包**:
默认启用、经整合包兼容性测试的精选插件集合。
_Avoid_: 默认全开、未筛选全装

**可选包**:
默认关闭、在启动器/设置中一键开启的插件集合，可能有边缘兼容风险。
_Avoid_: 默认全开

**可插拔性**:
整合包内一切组件（核心/可选插件、预设、模型↔预设联动）都遵循 DSH"一切皆插件"哲学，可随时插入与拆除；"核心包"只是默认组合，不是焊死的——任何插件（含核心）都可用官方插件机制移除。
_Avoid_: 把任意组件熔进基座不可移除

**补丁优先维护**:
兼容性修复以补丁文件承载并在构建/安装时应用；仅在大规模改动时才 fork 上游。
_Avoid_: 逐个 fork 全部入选插件

**目标用户**:
以 Windows 为主的非技术用户；macOS/Linux 为二等支持。
_Avoid_: 笼统的"所有人"
